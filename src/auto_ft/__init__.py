"""auto_ft package marker.

Version lookup uses importlib.metadata (single source of truth: pyproject.toml).
Do not define a module-level version attribute here — enforced by the anti-pattern
check in tests.
"""
